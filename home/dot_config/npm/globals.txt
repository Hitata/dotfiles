# npm global packages — managed by chezmoi
# One package per line. Blank lines and lines starting with # are ignored.
# Edit this file, then run `chezmoi apply` to sync installs.
#
# Note: fnm scopes globals per Node version. If you switch Node major version,
# re-run `chezmoi apply` to reinstall under the new version.

@anthropic-ai/claude-code
@google/gemini-cli
vercel
playwright
